#pragma once

#include <juce_gui_basics/juce_gui_basics.h>

float getStringWidth(const juce::String& string, const juce::Font& font);
float getMaxLineWidth(const juce::String& string, const juce::Font& font);
